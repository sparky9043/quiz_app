name: Hello World!

on:
    push:
        branches:
            - main

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: |
                echo "Hello World!"